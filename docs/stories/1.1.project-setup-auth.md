# Story 1.1: Project Setup & Authentication

## Status

Draft

## Story

**As a** user,
**I want** to access the dashboard securely with a token,
**so that** only I can see my content team data.

## Acceptance Criteria

1. Next.js 14 App Router project builds and runs with `npm run dev`
2. shadcn/ui is initialized with dark theme configuration
3. Login page at `/login` accepts a token and stores it in an httpOnly cookie
4. Protected API middleware validates AUTH_TOKEN on all `/api/*` routes
5. Unauthenticated requests redirect to `/login`
6. Environment variables are loaded from `.env.local`
7. Database connection pool is established and tested with a health check query
8. All 18 ct_* tables exist in the database (migration applied)

## Tasks / Subtasks

- [ ] Install shadcn/ui and initialize with dark theme (AC: 2)
  - [ ] Run `npx shadcn-ui@latest init` with dark theme
  - [ ] Add base components: Button, Card, Input, Badge, Dialog, Table, Tabs
- [ ] Create login page at `src/app/login/page.tsx` (AC: 3)
  - [ ] Token input form with shadcn/ui Input and Button
  - [ ] POST to `/api/auth/login` that validates token and sets httpOnly cookie
  - [ ] Redirect to `/` on success
- [ ] Create auth middleware at `src/middleware.ts` (AC: 4, 5)
  - [ ] Check `auth-token` cookie on all routes except `/login`, `/_next`, `/favicon.ico`
  - [ ] Redirect to `/login` if no valid token
- [ ] Create API auth helper at `src/lib/auth.ts` (AC: 4)
  - [ ] `validateRequest(request)` function that checks cookie
  - [ ] Use in all API route handlers
- [ ] Create `/api/auth/login` route (AC: 3)
  - [ ] Validate token against AUTH_TOKEN env var
  - [ ] Set httpOnly, secure, sameSite cookie
- [ ] Create `/api/health` route (AC: 7)
  - [ ] Run `SELECT NOW()` query to verify DB connection
  - [ ] Return `{ status: 'ok', timestamp: ... }`
- [ ] Apply database migration (AC: 8)
  - [ ] Document steps to run `001_content_team.sql` against Supabase
- [ ] Verify full build succeeds (AC: 1)
  - [ ] Run `npm run build` without errors

## Dev Notes

**Existing files to modify/use:**
- `src/lib/db.ts` - Database connection pool (already exists)
- `src/lib/types.ts` - TypeScript interfaces (already exists)
- `src/lib/utils.ts` - cn() utility (already exists)
- `src/app/layout.tsx` - Root layout (already exists, update with sidebar later)
- `src/app/globals.css` - Global styles (already exists with dark theme)
- `tailwind.config.ts` - Theme config (already exists with custom colors)
- `supabase/migrations/001_content_team.sql` - Schema (already exists)

**shadcn/ui setup:**
```bash
npx shadcn-ui@latest init
# Choose: TypeScript, tailwind.config.ts, src/components/ui, @/components
npx shadcn-ui@latest add button card input badge dialog table tabs dropdown-menu
```

**Auth token flow:**
1. User enters token on `/login`
2. POST `/api/auth/login` validates against `process.env.AUTH_TOKEN`
3. Sets httpOnly cookie `auth-token`
4. Middleware checks cookie on every route
5. API routes use `validateRequest()` helper

### Testing

- Test file location: `src/__tests__/api/auth.test.ts`
- Test login flow: valid token sets cookie, invalid returns 401
- Test middleware: unauthenticated redirects to /login
- Test health check: returns 200 with timestamp

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial story creation | Scrum Master |
