# Story 1.4: Agent Monitoring Page

## Status

Draft

## Story

**As a** user,
**I want** to see detailed status of all 13 agents and their task history,
**so that** I can monitor agent performance and troubleshoot issues.

## Acceptance Criteria

1. Agents page at `/agents` displays a card for each of the 13 agents
2. Each card shows: display name, role, status badge, last active time, pending task count
3. Clicking an agent card shows its recent task history (last 20 tasks)
4. Task history shows: title, status, created/completed timestamps, result summary
5. Agent cards are sorted by status (active first, then idle, then error)
6. API route `/api/agents` returns all agents with task counts

## Tasks / Subtasks

- [ ] Create `/api/agents` route (AC: 6)
  - [ ] Return all agents with pending_tasks count via subquery
  - [ ] Order by status DESC, display_name ASC
- [ ] Create `/api/agents/[slug]` route (AC: 3, 4)
  - [ ] Return agent details + last 20 tasks
- [ ] Create AgentCard component at `src/components/agents/agent-card.tsx` (AC: 1, 2)
  - [ ] Props: agent data with task count
  - [ ] Show: display_name, role, StatusBadge, formatRelativeTime(last_active_at), pending task count badge
  - [ ] Click handler to open detail view
- [ ] Create AgentDetail component at `src/components/agents/agent-detail.tsx` (AC: 3, 4)
  - [ ] shadcn/ui Dialog or slide-over panel
  - [ ] Task list table: title, status badge, created_at, completed_at
  - [ ] Result summary (collapsed, expandable)
- [ ] Assemble agents page at `src/app/agents/page.tsx` (AC: 1, 5)
  - [ ] Grid of AgentCards sorted by status
  - [ ] 30s polling for status updates

## Dev Notes

**Reuses from Story 1.3:** StatusBadge, usePolling hook

**Agent slugs (13 total):**
content-director, editor-chief, tech-chief, design-director, copywriter, content-curator, clone-agent, carousel-creator, listening-director, audience-director, channel-controller, relations-manager, content-searcher

**Depends on:** Story 1.1 (DB, auth), Story 1.2 (sidebar)

### Testing

- Test `/api/agents` returns 13 agents with task counts
- Test `/api/agents/content-director` returns agent with tasks
- Test AgentCard renders all required fields
- Test click opens detail panel

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-10 | 1.0 | Initial story creation | Scrum Master |
